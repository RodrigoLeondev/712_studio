export interface HeroContentProps {
  badgeText?: string;
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  animationDelay?: number;
}

export interface HeroVideoProps {
  videoUrl: string;
}
